<?php

class AuthController {
    // Métodos de autenticação como login, cadastro, logout
    // showLoginForm(), showCadastroForm(), login(dados), cadastro(dados), logout()
}


<?php

class AuthController {
    // Métodos de autenticação como login, cadastro, logout
    // showLoginForm(), showCadastroForm(), login(dados), cadastro(dados), logout()
}

 